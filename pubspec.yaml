name: flutter_cost_calc

description: flutter_cost_calc
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.3.1
  #http: ^0.12.1
  cupertino_icons: ^0.1.3
  flutter_button: ^0.0.4
  google_fonts: ^2.0.0
  step_progress_indicator: ^0.2.5+8
  page_transition: ^2.0.2
  sweetsheet: ^0.3.1
  sleek_circular_slider: ^2.0.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true